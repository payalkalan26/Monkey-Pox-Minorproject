# --- Python basics ---
__pycache__/
*.py[cod]
*.pyo

# Jupyter
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/
env/

# Databases (local only, not in repo)
*.db
*.sqlite3

# Models / weights
# keep only the final model used in the webapp
models/
webapp/models/
*.h5
webapp/*.h5
!webapp/models/
!webapp/models/monkeypox_mobilenetv3_final.h5

# User uploaded images at runtime (not the dataset)
static/uploads/
webapp/static/uploads/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/

# Dev-only folders (not needed for deployment)
assets/
samples/
scripts/

# Training / testing scripts (not needed in production)
train_model.py
train_model_v3.py
test_model.py
test_model_v3.py
app.py
